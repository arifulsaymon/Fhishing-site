import Link from 'next/link';import {platforms} from '@/lib/platforms';
export default function Home(){return <main className="container"><section className="hero"><h1>Social account recovery help</h1><p>Choose a platform to submit a support request. We never ask for or store your password, passcode, OTP, or recovery code.</p></section><div className="grid">{platforms.map(p=><Link className="card" href={'/'+p.slug} key={p.slug}><img className="logo" src={p.logoUrl} alt=""/><h3>{p.name}</h3><p className="muted">{p.description}</p><span className="btn secondary">Get help</span></Link>)}</div><div className="footer">SoHelpSocial · Account recovery assistance · Never share your password with support staff.</div></main>}
