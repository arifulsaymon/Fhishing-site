'use client';
import {useState} from 'react';import {getPlatform} from '@/lib/platforms';
export default function HelpPage(){const platform=getPlatform('xhelp')!;const [email,setEmail]=useState('');const [issue,setIssue]=useState('');const [status,setStatus]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setStatus('Submitting…');const r=await fetch('/api/requests',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({platform:platform.name,email,issue})});const j=await r.json();setStatus(j.message||'Submitted.')}return <main className="container"><div className="form"><img className="logo" src={platform.logoUrl} alt=""/><h1>{platform.name} recovery help</h1><p className="muted">Tell us what happened. For your security, never enter a password, verification code, recovery code, or security key here.</p><form onSubmit={submit}><div className="field"><label>Contact email</label><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></div><div className="field"><label>What do you need help with?</label><textarea required value={issue} onChange={e=>setIssue(e.target.value)} placeholder="Example: I cannot access my account because…"/></div><button className="btn" type="submit">Submit support request</button></form>{status&&<div className="success">{status}</div>}<div style={{marginTop:18}}><a className="btn secondary" href={platform.recoveryUrl} target="_blank" rel="noreferrer">Open official {platform.name} recovery</a></div></div></main>}
